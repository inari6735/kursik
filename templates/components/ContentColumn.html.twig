{# Reading column pinned to the left edge (next to the sidebar) instead of the
   default centered container. Used by pages overriding the `main` block. #}
<div class="max-w-3xl">
    <twig:Layout:Flashes />
    {% block content %}{% endblock %}
</div>
