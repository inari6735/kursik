{% extends 'base.html.twig' %}

{% block title %}New course — kursik{% endblock %}

{% block body %}
    <twig:PageHeader title="New course" />

    <twig:Card>
        {{ form_start(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.description) }}

            <twig:EditorField label="Content" uploadUrl="{{ path('course_image_upload') }}" attachUrl="{{ path('course_file_upload') }}">
                {{ form_widget(form.content, {attr: {'data-editor-target': 'input'}}) }}
            </twig:EditorField>

            <twig:FormActions>
                <twig:Button>Create course</twig:Button>
                <twig:Button variant="ghost" href="{{ path('course_index') }}">Cancel</twig:Button>
            </twig:FormActions>
        {{ form_end(form) }}
    </twig:Card>
{% endblock %}
